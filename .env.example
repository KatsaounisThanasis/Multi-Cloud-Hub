# ================================================================
# Multi-Cloud Manager v3.0 - Environment Configuration Template
# ================================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual credentials below
# 3. NEVER commit .env to git (it's in .gitignore)
#
# ================================================================

# ----------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------
# PostgreSQL password for Docker Compose
# Choose a strong password for production
DB_PASSWORD=your_secure_db_password_here

# ----------------------------------------------------------------
# Redis Configuration
# ----------------------------------------------------------------
# Redis URL for Celery task queue
# Default works with Docker Compose setup
REDIS_URL=redis://redis:6379/0

# ----------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------
# Application environment: development, staging, production
ENVIRONMENT=development

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ----------------------------------------------------------------
# Security Configuration
# ----------------------------------------------------------------
# JWT Secret Key for authentication tokens
# REQUIRED for production - generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your_jwt_secret_key_here

# Allowed CORS origins (comma-separated)
# Use specific origins in production, e.g.: https://yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ----------------------------------------------------------------
# AZURE CREDENTIALS
# ----------------------------------------------------------------
# How to get Azure credentials:
# 1. Go to: https://portal.azure.com
# 2. Azure Active Directory → App registrations → New registration
# 3. Note the Application (client) ID and Directory (tenant) ID
# 4. Certificates & secrets → New client secret → Copy the value
# 5. Subscriptions → Copy your Subscription ID
#
# Required for Azure deployments:
AZURE_SUBSCRIPTION_ID=00000000-0000-0000-0000-000000000000
AZURE_TENANT_ID=00000000-0000-0000-0000-000000000000
AZURE_CLIENT_ID=00000000-0000-0000-0000-000000000000
AZURE_CLIENT_SECRET=your_azure_client_secret_here

# ----------------------------------------------------------------
# AWS CREDENTIALS (Optional - for future AWS support)
# ----------------------------------------------------------------
# How to get AWS credentials:
# 1. Go to: https://console.aws.amazon.com/iam/
# 2. Users → Your user → Security credentials
# 3. Create access key → Copy Access Key ID and Secret Access Key
#
# Leave empty if not using AWS:
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# ----------------------------------------------------------------
# GOOGLE CLOUD PLATFORM (GCP) CREDENTIALS
# ----------------------------------------------------------------
# How to get GCP credentials:
# 1. Go to: https://console.cloud.google.com
# 2. IAM & Admin → Service Accounts → Create Service Account
# 3. Grant "Editor" role (or specific roles needed)
# 4. Create key → JSON → Download the JSON file
# 5. Save it as: ./credentials/gcp-service-account.json
# 6. Copy your Project ID from the GCP Console
#
# Required for GCP deployments:
GOOGLE_PROJECT_ID=your-gcp-project-id
GOOGLE_REGION=us-central1

# Path to GCP service account JSON file (relative to project root)
# The file should be in credentials/ directory (ignored by git)
GOOGLE_APPLICATION_CREDENTIALS=./credentials/gcp-service-account.json

# ----------------------------------------------------------------
# SECURITY NOTES
# ----------------------------------------------------------------
# ⚠️  IMPORTANT SECURITY REMINDERS:
#
# 1. NEVER commit this .env file to version control
# 2. Use different credentials for dev/staging/production
# 3. Rotate secrets regularly (every 90 days minimum)
# 4. Use Azure Key Vault or AWS Secrets Manager in production
# 5. Limit service principal/account permissions to minimum required
# 6. Enable MFA on all cloud provider accounts
# 7. Monitor credential usage with cloud provider audit logs
#
# ----------------------------------------------------------------

# ================================================================
# OPTIONAL: Advanced Configuration
# ================================================================
# Uncomment and configure if needed:

# API Authentication (for production deployments)
# API_AUTH_ENABLED=false
# API_KEY=your_secure_api_key_here

# Rate Limiting
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_REQUESTS=60

# CORS Origins (comma-separated for production)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Terraform State Backend (for team collaboration)
# TERRAFORM_STATE_S3_BUCKET=my-terraform-state-bucket
# TERRAFORM_STATE_GCS_BUCKET=my-terraform-state-bucket
# TERRAFORM_STATE_STORAGE_ACCOUNT=myterraformstate

# ================================================================
# End of Configuration
# ================================================================
